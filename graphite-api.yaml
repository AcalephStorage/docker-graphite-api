finders:
  - graphite_influxdb.InfluxdbFinder
influxdb:
   host: influxdb
   port: 8086
   user: graphite
   pass: graphite
   db:   graphite
   schema:
     - ['.*', 30]
     - ['5m.*', 300]
     - ['1h.*', 3600]
cache:
    CACHE_TYPE: 'filesystem'
    CACHE_DIR: '/tmp/graphite-api-cache'
